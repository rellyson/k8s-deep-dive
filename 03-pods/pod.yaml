apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    app: redis
spec:
  containers:
    - name: redis-db
      image: redis:7-alpine
      command:
        - "redis-server"
      args:
        - "--logLevel debug"
